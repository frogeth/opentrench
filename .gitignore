node_modules/
dist/
backend/config.json
.env
.DS_Store
backend/state.json
backend/state.json.tmp
backend/backend.log

# plugins the user installed (code from other people; never part of the repo)
backend/plugins/
# and what they stored: their own data and the settings the user typed in, secrets included
backend/plugins-state.json
backend/plugins-state.json.tmp

# Claude working files: design specs, plans and the desktop launch config stay local
docs/superpowers/
.claude/
